name: tecnired
description: "App GPS Enterprise con JWT"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  # 🌐 HTTP requests
  http: ^0.13.6

  # 💾 Persistencia de sesión (TOKEN)
  shared_preferences: ^2.2.2

  # 🎨 Iconos iOS
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true